const Navigation=(probs)=>{

        const {data,setdata}=probs;
        return (
                <div>
               <h2>Navigation:{data}</h2>
                </div>
        )
        }

export default Navigation;